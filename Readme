


# Text-to-Audio Converter (T2S)

T2S is a straightforward Python application designed to convert text input into audio files. It employs text-to-speech synthesis to produce audio from text.

## Requirements
- Python
- gTTS (Google Text-to-Speech)

## Usage
1. Execute `python manage.py runserver` to create the audio file.
2. Locate the resulting audio file in the `output` directory.
