#Text-to-Audio Converter (T2S)

T2S is a simple Python application that converts text input into audio files. It utilizes text-to-speech synthesis to generate audio from text.

##Requirements
- Python
- gTTS (Google Text-to-Speech)

##Usage
1. Run `python manage.py runserver` to generate the audio file.
2. Find the output audio file in the `output` directory.
